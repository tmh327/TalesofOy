using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mobParam : MonoBehaviour
{
    public string data = "";
    // Start is called before the first frame update
    void Start()
    {
        //data = "thisID -500dmg";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
